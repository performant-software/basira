{
  "name": "BASIRA",
  "license": "MIT",
  "engines": {
    "node": "18.15.0",
    "yarn": "1.x"
  },
  "scripts": {
    "build": "export NODE_OPTIONS=--openssl-legacy-provider && yarn --cwd client install && yarn --cwd client run build && cd ..",
    "deploy": "cp -a client/build/. public/",
    "heroku-postbuild": "export NODE_OPTIONS=--openssl-legacy-provider && yarn build && yarn deploy"
  }
}
